(self.webpackChunkshu_moe=self.webpackChunkshu_moe||[]).push([[818],{5379:s=>{"use strict";s.exports='<h2 id="类型检查与拷贝"><a href="#%E7%B1%BB%E5%9E%8B%E6%A3%80%E6%9F%A5%E4%B8%8E%E6%8B%B7%E8%B4%9D" aria-hidden="true" tabindex="-1"><span class="icon icon-link"></span></a>类型检查与拷贝</h2>\n<p>JS 的数据类型可以大致分为两种，<br>\n一种是值类型，引用值的变量之间不会产生相互影响；<br>\n还有一种是引用类型，变量只保存引用，可能存在相互影响。<br>\n接下来介绍一下两种类型：</p>\n<pre><code class="hljs language-js"><span class="hljs-comment">// 值类型</span>\n<span class="hljs-keyword">const</span> a = <span class="hljs-number">1</span>; <span class="hljs-comment">// a 的值是 1</span>\n<span class="hljs-keyword">let</span> b = a; <span class="hljs-comment">// b 的值也是 1，a === b</span>\nb = <span class="hljs-number">2</span>; <span class="hljs-comment">// b 的值修改成了 2，a 的值还是 1，a !== b</span>\n<span class="hljs-keyword">const</span> c = <span class="hljs-number">1</span>; <span class="hljs-comment">// c 的值是 1，a === c</span>\n\n<span class="hljs-comment">// 引用类型</span>\n<span class="hljs-keyword">const</span> a = {<span class="hljs-attr">a</span>: <span class="hljs-number">1</span>}; <span class="hljs-comment">// a 变量保存了 {a: 1} 的引用</span>\n<span class="hljs-keyword">let</span> b = a; <span class="hljs-comment">// b 保存同样一份引用，两个变量 a 和 b 指向同一个对象值，b === a</span>\nb = {<span class="hljs-attr">a</span>: <span class="hljs-number">1</span>}; <span class="hljs-comment">// b 指向一个新声明的变量，对象变量之间的比较，比较引用的值是否是同一个，b !== a</span>\n<span class="hljs-keyword">const</span> c = a; <span class="hljs-comment">// a === c</span>\nc.b = <span class="hljs-number">2</span>; <span class="hljs-comment">// 通过变量 c 修改引用值</span>\na; <span class="hljs-comment">// {a: 1, c: 2}</span></code></pre>\n<p>在这个前提之下，拷贝分为浅拷贝和深拷贝两种。<br>\n直接获取一个变量的对象值引用，称为浅拷贝操作，会获得一个旧对象值的新引用，任何对旧对象值本身的操作，都会体现到对象值的所有引用变量上；<br>\n通过旧对象值生成新对象值，称为深拷贝操作，会获得一个新对象值，对其修改不会对旧对象值产生影响。</p>\n<pre><code class="hljs language-js"><span class="hljs-comment">// 判断一个数据是否是数组</span>\n<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">isArray</span>(<span class="hljs-params">arr</span>) </span>{\n  <span class="hljs-keyword">if</span> (<span class="hljs-keyword">typeof</span> <span class="hljs-built_in">Array</span>.isArray === <span class="hljs-string">\'function\'</span>) <span class="hljs-keyword">return</span> <span class="hljs-built_in">Array</span>.isArray(arr)\n  <span class="hljs-keyword">else</span> <span class="hljs-keyword">return</span> {}.toString.call(arr) === <span class="hljs-string">\'[object Array]\'</span>\n}\n<span class="hljs-comment">// 深拷贝</span>\n<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">deepClone</span>(<span class="hljs-params">source</span>) </span>{\n  <span class="hljs-comment">// 如果是一个数组，针对数组中的每一项递归执行深拷贝</span>\n  <span class="hljs-keyword">if</span> (isArray(source)) <span class="hljs-keyword">return</span> source.map(<span class="hljs-function">(<span class="hljs-params">item</span>) =></span> deepClone(item))\n  <span class="hljs-comment">// 如果是一个对象，针对每一个 key 的 value 递归</span>\n  <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-keyword">typeof</span> source === <span class="hljs-string">\'object\'</span> &#x26;&#x26; source !== <span class="hljs-literal">null</span>) {\n    <span class="hljs-keyword">const</span> target = {}\n    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> i <span class="hljs-keyword">in</span> source) {\n      target[i] = deepClone(source[i])\n    }\n    <span class="hljs-keyword">return</span> target\n  }\n  <span class="hljs-comment">// 如果是其他数据类型，包含 null, undefined, number, string, boolean, bigint, symbol 不存在引用类型问题，可以直接返回</span>\n  <span class="hljs-keyword">else</span> <span class="hljs-keyword">return</span> source\n}</code></pre>\n<h2 id="日期格式化"><a href="#%E6%97%A5%E6%9C%9F%E6%A0%BC%E5%BC%8F%E5%8C%96" aria-hidden="true" tabindex="-1"><span class="icon icon-link"></span></a>日期格式化</h2>\n<p>现代浏览器对日期时间操作的支持其实很完善，为体积考虑可以尽量避免 lodash</p>\n<pre><code class="hljs language-js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">timeFormat</span>(<span class="hljs-params">timestamp, format = <span class="hljs-string">\'Y/M/D h:m:s\'</span></span>) </span>{\n  <span class="hljs-keyword">const</span> d = <span class="hljs-keyword">new</span> <span class="hljs-built_in">Date</span>(timestamp)\n  <span class="hljs-keyword">if</span> (d + <span class="hljs-string">\'\'</span> === <span class="hljs-string">\'Invalid Date\'</span>) <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-built_in">Error</span>(<span class="hljs-string">\'Invalid Date\'</span>)\n\n  <span class="hljs-keyword">const</span> list = {\n    <span class="hljs-attr">Y</span>: d.getFullYear(),\n    <span class="hljs-attr">M</span>: d.getMonth() + <span class="hljs-number">1</span>,\n    <span class="hljs-attr">D</span>: d.getDate(),\n    <span class="hljs-attr">h</span>: d.getHours(),\n    <span class="hljs-attr">m</span>: d.getMinutes(),\n    <span class="hljs-attr">s</span>: d.getSeconds(),\n  }\n\n  <span class="hljs-keyword">return</span> format.replace(<span class="hljs-regexp">/[Y|M|D|h|m|s]/g</span>, <span class="hljs-function">(<span class="hljs-params">match</span>) =></span>\n    (list[match] + <span class="hljs-string">\'\'</span>).padStart(<span class="hljs-number">2</span>, <span class="hljs-string">\'0\'</span>)\n  )\n}\n\n<span class="hljs-built_in">console</span>.log(timeFormat(<span class="hljs-built_in">Date</span>.now()))</code></pre>\n<h2 id="符号表达式"><a href="#%E7%AC%A6%E5%8F%B7%E8%A1%A8%E8%BE%BE%E5%BC%8F" aria-hidden="true" tabindex="-1"><span class="icon icon-link"></span></a>符号表达式</h2>\n<p>用在书写判断条件，或者执行顺序上面</p>\n<pre><code class="hljs language-js">+[] <span class="hljs-comment">// 转换为 number 类型，[] 转换结果为 0</span>\n<span class="hljs-string">\'\'</span>+[] <span class="hljs-comment">// 转换为 string 类型，[] 转换结果为 \'\'</span>\n!![] <span class="hljs-comment">// 转换为 boolean 类型，[] 结果为 true</span>\n~-<span class="hljs-number">1</span> <span class="hljs-comment">// 按位非，~-1 结果为 0，我个人反应不过来这个</span>\n<span class="hljs-literal">true</span> &#x26;&#x26; <span class="hljs-built_in">console</span>.log(<span class="hljs-string">\'true\'</span>) <span class="hljs-comment">// &#x26;&#x26; 在第一个表达式 bool true 的情况下执行第二个表达式</span>\n<span class="hljs-literal">false</span> || <span class="hljs-built_in">console</span>.log(<span class="hljs-string">\'false\'</span>) <span class="hljs-comment">// || 在第一个表达式 bool false 的情况下执行第二个表达式</span></code></pre>\n<h2 id="闭包"><a href="#%E9%97%AD%E5%8C%85" aria-hidden="true" tabindex="-1"><span class="icon icon-link"></span></a>闭包</h2>\n<p>变量是存在作用域的概念的，以函数作为边界，不可以越界访问。<br>\n可以使用这个特性控制私有变量的访问方式。</p>\n<pre><code class="hljs language-js"><span class="hljs-keyword">const</span> [get, up, down] = (<span class="hljs-function">() =></span> {\n  <span class="hljs-keyword">let</span> start = <span class="hljs-number">0</span>\n  <span class="hljs-keyword">return</span> [\n    <span class="hljs-function">() =></span> start,\n    <span class="hljs-function">() =></span> start += <span class="hljs-number">1</span>,\n    <span class="hljs-function">() =></span> start -= <span class="hljs-number">1</span>,\n  ]\n})()</code></pre>\n<h2 id="this"><a href="#this" aria-hidden="true" tabindex="-1"><span class="icon icon-link"></span></a>this</h2>\n<p><sup>TODO</sup></p>\n<h2 id="循环"><a href="#%E5%BE%AA%E7%8E%AF" aria-hidden="true" tabindex="-1"><span class="icon icon-link"></span></a>循环</h2>\n<p>没见过有人不会循环，这个算基础了吧</p>\n<p>最简单的累计循环</p>\n<pre><code class="hljs language-js"><span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> i = <span class="hljs-number">0</span>; i &#x3C; <span class="hljs-number">10</span>; i += <span class="hljs-number">1</span>) {\n  <span class="hljs-built_in">console</span>.log(i)\n}</code></pre>\n<h3 id="遍历对象"><a href="#%E9%81%8D%E5%8E%86%E5%AF%B9%E8%B1%A1" aria-hidden="true" tabindex="-1"><span class="icon icon-link"></span></a>遍历对象</h3>\n<ol>\n<li>for in 遍历一个对象\n由于 <a href="https://bonsaiden.github.io/JavaScript-Garden/zh/#object.forinloop">for in 会获取原型链上面的属性</a>，所以需要使用 <code>hasOwnProperty</code> 进行检查，或者使用 <code>Object.create(null)</code> 创建一个完全空的对象用于数据存储。</li>\n</ol>\n<pre><code class="hljs language-js"><span class="hljs-keyword">const</span> list = {<span class="hljs-attr">a</span>: <span class="hljs-number">1</span>, <span class="hljs-attr">b</span>: <span class="hljs-number">2</span>, <span class="hljs-attr">c</span>: <span class="hljs-number">3</span>}\n<span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> key <span class="hljs-keyword">in</span> list) {\n  <span class="hljs-keyword">if</span> ({}.hasOwnProperty.call(list, key)) {\n    <span class="hljs-built_in">console</span>.log(key, list[key])\n  }\n}</code></pre>\n<ol start="2">\n<li>使用 keys 方法遍历一个对象 <code>Object.keys</code></li>\n</ol>\n<pre><code class="hljs language-js"><span class="hljs-keyword">const</span> list = {<span class="hljs-attr">a</span>: <span class="hljs-number">1</span>, <span class="hljs-attr">b</span>: <span class="hljs-number">2</span>, <span class="hljs-attr">c</span>: <span class="hljs-number">3</span>}\n<span class="hljs-built_in">Object</span>.keys(list).map(<span class="hljs-function"><span class="hljs-params">key</span> =></span> { <span class="hljs-built_in">console</span>.log(key, list[key]) })</code></pre>\n<ol start="3">\n<li>for of 循环一个可迭代对象</li>\n</ol>\n<pre><code class="hljs language-js"><span class="hljs-keyword">const</span> list = [<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>,<span class="hljs-number">4</span>]\n<span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> item <span class="hljs-keyword">of</span> list) {\n  <span class="hljs-built_in">console</span>.log(item)\n}</code></pre>\n<h3 id="遍历数组"><a href="#%E9%81%8D%E5%8E%86%E6%95%B0%E7%BB%84" aria-hidden="true" tabindex="-1"><span class="icon icon-link"></span></a>遍历数组</h3>\n<ol>\n<li>map 数组遍历，这个方法有返回值</li>\n</ol>\n<pre><code class="hljs language-js"><span class="hljs-keyword">const</span> arr = <span class="hljs-built_in">Array</span>.from({<span class="hljs-attr">length</span>: <span class="hljs-number">3</span>}) <span class="hljs-comment">// 生成长度为 3 的数组</span>\narr.map(<span class="hljs-function">(<span class="hljs-params">_, index</span>) =></span> index + <span class="hljs-number">1</span>)</code></pre>\n<ol start="2">\n<li>forEach 数组遍历，这个方法没有返回值</li>\n</ol>\n<pre><code class="hljs language-js">[<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>].forEach(<span class="hljs-built_in">console</span>.log)</code></pre>\n<h3 id="字符串"><a href="#%E5%AD%97%E7%AC%A6%E4%B8%B2" aria-hidden="true" tabindex="-1"><span class="icon icon-link"></span></a>字符串</h3>\n<p>可以在字符串中，按照规则匹配相同类型的值</p>\n<pre><code class="hljs language-js"><span class="hljs-string">`?a=1&#x26;b=2&#x26;c=3`</span>.replace(<span class="hljs-regexp">/[^\\?&#x26;]*=[^\\?&#x26;]*/g</span>, <span class="hljs-function"><span class="hljs-params">match</span> =></span> {\n  <span class="hljs-keyword">const</span> arr = match.split(<span class="hljs-string">\'=\'</span>)\n  <span class="hljs-built_in">console</span>.log(<span class="hljs-string">`key: <span class="hljs-subst">${arr[<span class="hljs-number">0</span>]}</span>, value: <span class="hljs-subst">${arr[<span class="hljs-number">1</span>]}</span>`</span>)\n  <span class="hljs-keyword">return</span> <span class="hljs-string">\'\'</span>\n})</code></pre>\n<h2 id="正则"><a href="#%E6%AD%A3%E5%88%99" aria-hidden="true" tabindex="-1"><span class="icon icon-link"></span></a>正则</h2>\n<p><sup>TODO</sup></p>\n'}}]);